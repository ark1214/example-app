machine:
  environment:
    NODE_ENV: production
  node:
    version: v6.10.2
  services:
    - postgresql

database:
  override:
    - createdb todo
    - echo "CREATE ROLE prod_user WITH LOGIN NOINHERIT ENCRYPTED PASSWORD 'prod12134';" | psql -U postgres
    - echo "Grant all privileges on database todo to prod_user;" | psql -U postgres